import { Title } from "@angular/platform-browser";

export interface IgetSongs {
   id:string,
   songTitle:string,
    albumTitle: string,
    composser: string,
   lyricist:string,
  url: string
}
export interface IgetSongsUrl {
  url:string
}
